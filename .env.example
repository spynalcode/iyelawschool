# Copy this file to ".env" for local development, or set these as
# Environment Variables in Vercel/Netlify when you deploy.
#
# Leave them blank to run in OFFLINE MODE (single device, no sync).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_ADMIN_CODE=IYE_ADMIN_2026
